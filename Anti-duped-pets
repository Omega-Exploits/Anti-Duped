<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Animated Blurred Loading Screen</title>
<style>
  body,html {margin:0;height:100%;font-family:sans-serif;background:#111;color:white;}
  .overlay {position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(0,0,0,0.4);backdrop-filter:blur(10px);transition:opacity 0.6s ease;}
  .card {background:rgba(255,255,255,0.05);padding:20px;border-radius:10px;text-align:center;min-width:300px;box-shadow:0 0 20px rgba(0,0,0,0.3);transition:transform 0.6s ease;}
  .upper-bar {margin-bottom:10px;color:#ccc;}
  .bar {height:14px;background:rgba(255,255,255,0.1);border-radius:999px;overflow:hidden;position:relative;}
  .fill {height:100%;width:0;background:linear-gradient(270deg,#6366f1,#60a5fa,#6366f1);
         background-size:200% 100%;animation:flow 2s linear infinite;
         border-radius:999px;transition:width 1.5s cubic-bezier(0.4,0,0.2,1);}
  .percent {margin-top:6px;font-weight:bold;}
  .complete-msg {font-size:1.2em;font-weight:bold;color:#90ee90;display:none;}

  @keyframes flow {
    0% {background-position:200% 0;}
    100% {background-position:-200% 0;}
  }
</style>
</head>
<body>
<div class="overlay" id="overlay">
  <div class="card" id="card">
    <div class="upper-bar" id="upperText">Loading — please wait</div>
    <div class="bar"><div class="fill" id="progressFill"></div></div>
    <div class="percent" id="percent">1%</div>
    <div class="complete-msg" id="completeMsg">Unduping Complete!</div>
  </div>
</div>
<script>
let progress = 1;
const fill = document.getElementById('progressFill');
const percentEl = document.getElementById('percent');
const upperText = document.getElementById('upperText');
const completeMsg = document.getElementById('completeMsg');

function setProgress(n){
  progress = Math.min(100, n);
  fill.style.width = progress + '%';
  percentEl.textContent = Math.round(progress) + '%';
  if(progress >= 100){
    upperText.style.display = 'none';
    percentEl.style.display = 'none';
    fill.style.width = '100%';
    completeMsg.style.display = 'block';
  }
}

setProgress(progress);
const timer = setInterval(()=>{
  setProgress(progress + 10);
  if(progress >= 100){
    clearInterval(timer);
  }
}, 20000);
</script>
</body>
</html>
